docs/
node_modules/
.DS_Store

# PlatformIO build output
.pio/

# PlatformIO regenerates these on open and they hold absolute local paths.
# extensions.json is kept: it names the extensions this project needs.
.vscode/*
!.vscode/extensions.json

# Regenerable pipeline intermediates (large). Rebuild with `npm run all`.
data/people.json
data/quotes-raw.json

# Device export, regenerable from quotes-by-day.json with `npm run export:device`.
data/device/
